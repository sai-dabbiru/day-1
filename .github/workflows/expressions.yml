name: Expressions & Contexts Demo
on: [workflow_dispatch , push]
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Github Expression step-1
        run: echo "${{ 'This is an expression'}}"
      - name: Github Context-1
        run: echo "${{ github }}"
      - name: Ouput Commands-1
        run: echo "${{ toJSON(github.ref) }}"
      - name: Ouput Commands-2
        run: echo "${{ runner.name && runner.os}}"